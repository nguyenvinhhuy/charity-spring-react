package com.clb.charity.reaction.domain;

/** The fixed set of reactions a member may pick, mirroring Microsoft Teams' reaction bar. */
public enum ReactionType {
    LIKE,
    LOVE,
    CELEBRATE,
    LAUGH,
    SURPRISED,
    SAD
}
