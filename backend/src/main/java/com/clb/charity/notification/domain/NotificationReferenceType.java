package com.clb.charity.notification.domain;

/** What kind of resource a notification's referenceId points to, if any. */
public enum NotificationReferenceType {
    CAMPAIGN,
    POST,
    INQUIRY
}
